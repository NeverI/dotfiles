- clean:
  - '~'
  - '~/.config'

- link:
    ~/.config/dotfiles: ''
    ~/.profile: profile
    ~/.bashrc: bashrc
    ~/.bash_logout: bash_logout
    ~/.config/liquidpromptrc: bash/liquidpromptrc

- shell:
    - git submodule update --init --recursive
