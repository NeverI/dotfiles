#!/usr/bin/env bash

file=$(cat ~/.quickedit  | dmenu -i)

[[ -n $file ]] || exit

recent_list "$file" ~/.quickedit

fullpath=$(echo "$file" | sed "s,~,$HOME,")
cmd="nvim $fullpath"

if [[ "$fullpath" =~ !$ ]]; then
  cmd=$(echo "sudo $cmd" | cut -d'!' -f1)
fi

st -n FloatMe -e $cmd
